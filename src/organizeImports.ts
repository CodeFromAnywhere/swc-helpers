/**
 * clone https://github.com/simonhaenisch/prettier-plugin-organize-imports and try to rewrite that into modern typescript, as efficiently as possible
 *
 * this will make it possible to split bigger files into multiple smaller files.
 */
export const organizeImports = (code: string): string => {
  return code;
};
